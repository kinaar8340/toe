# =============================================================
# Aaron’s Theory of Everything (TOE) v1.0.0
# Requirements file — production-ready + fully reproducible
# =============================================================

# Core Runtime (always required)
torch>=2.11.0                   # PyTorch 2.4+ (CPU or CUDA)
numpy>=2.4.4
pandas>=3.0.2                   # Results DataFrames and CSV export
scipy>=1.17.1                   # Used in PDE solver and Laguerre-Gaussian modes
seaborn>=0.13.2                 #
matplotlib>=3.10.8              # Plotting stability islands and gauge evolution
tqdm>=4.67.3                    # Progress bars in sweeps and PDE relaxation
omegaconf>=2.3.0                # YAML config loading (configs/default.yaml)
ray>=2.55.0                     # API for building distributed applications https://github.com/ray-project/ray
types-pyyaml>=6.0.12            # Typing stubs for PyYAML https://github.com/python/typeshed
tabulate>=0.10.0                # Pretty-print tabular data https://github.com/astanin/python-tabulate
optuna>=4.8.0                   # A hyperparameter optimization framework https://optuna.org/



# Optional but recommended for full reproduction
sentence-transformers>=5.4.1   # Fact embedding (used in some qvpic tests)
scikit-learn>=1.8.0             # FastICA and clustering utilities

# =============================================================
# Installation instructions
# =============================================================
#
# 1. Basic CPU install:
#    pip install -r requirements.txt
#
# 2. CUDA 12.1 / 12.4 (recommended for speed):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#    pip install -r requirements.txt
#
# 3. Verify reproduction works:
#    python scripts/run_reproduction.py --trials 30
#
# This will confirm:
#   • W_g = 111.408 locked to 4 decimal places
#   • Braiding-phase attractor φ_b ≈ 0.8145
#   • Stability islands (active_cubes vs pseudo-Z)
#
# All outputs saved to outputs/reproduction/
# =============================================================
